# This is a small project demo the data crawling feature

Use `pnpm` package manager

Move to the root project

Run the command `pnpm install`

Run `node server.js` or `node index.js` to enjoy the results
